server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/AlejandroTorresUNMSM/bootcamp-config
          search-paths: /
          default-label: "main"